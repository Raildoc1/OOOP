#include "pch.h"
#include "../Lab1/TritSet.h"

using namespace tritset;
using namespace tritsBits;

TEST(TritSetTest, TritSetStartsUnknown) {
	TritSet set(1000);
	/*for (int i = 0; i < 1000; i++) {
		EXPECT_EQ(set[i], U);
	}*/
}